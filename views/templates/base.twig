<!DOCTYPE html>
<html>
  <head>
    {{function('wp_head')}}
    <meta charset="utf-8">
    <title>{{site.title}}</title>
    <link rel="stylesheet" href="{{theme.link}}/dist/css/theme.css">
  </head>
  <body>
    <h1>Test</h1>

    <div class="container">
      <div class="row">
        {% for book in books %}
          <div class="col-lg-4">
            {% include 'molecules/card.twig' %}
          </div>
        {% endfor %}
      </div>
    </div>

    {{ function('wp_footer') }}
  </body>
</html>
